#!/bin/bash
set -euxo pipefail

curl --proto '=https' --tlsv1.2 -sSf https://sh.rustup.rs | sh -s -- -y --no-modify-path
"$HOME/.cargo/bin"/rustup component add rust-src
sed 's/#.*//' "$DOTFILES_ROOT/list/cargo" | xargs "$HOME/.cargo/bin"/cargo install
